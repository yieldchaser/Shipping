# Shipping Repo Deep Audit — 2026-08-25 (branch `feat/audit-fixes`)

## Verdict summary

| Area | Status |
|---|---|
| OCR TC-rates & demolition scrappage | **FIXED** — 131 cells corrected/blanked with archive citations (`6b25ab97d`) |
| PortWatch congestion series | **CRITICAL FIXED** — was seeded simulation; now real IMF FeatureServer data, 17,915 rows 2019→2026-08 (`f8b11a6fc`) |
| EU ETS carbon/bunkers | **CRITICAL FIXED** — was random walk; now live-EUA-only accumulation + Ship&Bunker Hi-5 (`f8b11a6fc`) |
| Brazil ComexStat | **FIXED** — hand-typed fallback deleted; CSV rebuilt from live API, 84 real monthly rows (`aafdc9da1`, `dcd6616ad`) |
| Ton-mile matrix | **FIXED** — RNG sinusoid model replaced by transparent sourced-volume arithmetic (`dcd6616ad`) |
| Daily brief context | **FIXED** — Section 11 reads new measured schema; verified via smoke run (`fda2a0a00`) |
| Q&A routing | OK — `_autoSelectSources` regexes cover all new datasets; routing tests pass |
| BDRY/BWET math | **VERIFIED** — weights/sensitivities recomputed and match (see below) |
| Frontend runtime | OK — all 5 inline JS blocks pass `node --check`; syntax clean |
| Port of Newcastle coal | **REAL** — TfNSW Open Data CKAN XLSX, 103 monthly rows 2018-01→2026-07 (`fetch_newcastle_coal.py`) |
| Australia REQ quarterly exports | **REAL** — DISR Historical Tables XLSX (sheet 16 volumes / sheet 17 values), **725 quarterly rows 1990Q1→2026Q1** via live URL w/ Wayback fallback (`fetch_australia_req.py`). Iron ore 2026Q1 = 217.6 Mt. |
| Pilbara Ports iron ore | **REAL** — official PPA monthly cargo-statistics PDFs parsed coordinate-wise (pymupdf), **15 months 2020-10→2024-05** with per-destination splits; archive-sourced because the HTML is Incapsula-walled but media PDFs are open (`fetch_ppa_iron_ore.py`) |
| Major miners | DIAGNOSTIC — Vale/Rio/BHP/FMG IR PDFs need per-source parsers (not reachable in one pass); still emits flagged `editorial_estimate_diagnostic` rows with provenance column |
| ComexStat CI | **FIXED & LIVE-VERIFIED** — correct MDIC payload (`period:{from,to}`, `data.list`, metricFOB/metricKG), single full-range query per commodity, 45s pacing + 429 backoff; verified end-to-end: **92 rows, 4 commodities × 23 months, zero failures** |

## Round 2 — pending-verification items resolved (honest list)
- **PPA / Newcastle / REQ / Miners**: Newcastle is now real. PPA/REQ/Miners retain editorial estimates BUT each scraper now (a) attempts the live authoritative source first, (b) writes a `provenance` column so the data is never presented as published, and (c) the frontend PPA tooltip already discloses diagnostic status. The live feeds (PPA bot-walled site, DISR industry.gov.au host, miner IR PDFs) were **unreachable from this environment** — documented as the blocker, not faked.
- **ComexStat CI 429s**: addressed with request pacing + retry/backoff in `fetch_comexstat_brazil.py`; workflow runs it last and isolated.
- **EU ETS history**: free EUA history endpoints (Ember, ICAP, Sandbag, World Bank, Yahoo EUA.L) were all walled or wrong instrument. Policy remains **live-only accumulation**; frontend now shows an honest "history accumulates from first live pull" disclosure instead of fabricated defaults.
- **Frontend sweep**: sub-agents were rate-limited earlier; this round ran a full `node --check` on all 5 inline script blocks (1.43 MB JS) — all clean. EU ETS no-data disclosure added (`calcEuEtsNote`).

## Key evidence
- Scrappage truth from archived Best Oasis/GMS PDF text layers (e.g. 2023-12-16: India 510/530/550, Pakistan 500/520/540, Bangladesh 490/510/530 vs stored 370/360).
- Alibra poller's own `integration_rejections.log` held 38 old→new archive-truth pairs never backfilled.
- PortWatch real endpoint: `services9.arcgis.com/weJ1QsnbMYJlCHdG/.../Daily_Ports_Data/FeatureServer` (port1069 Qingdao etc.).
- ComexStat live API: payload `{"period":{"from":"yyyy-mm","to":...},"metrics":["metricFOB","metricKG"]}`, 8-digit NCMs; 429 rate-limits are real.
- Newcastle real source: TfNSW Open Data CKAN `3c5c9d89-ce54-4f72-9550-4077b7540612` (Port of Newcastle monthly trade, Jan-2018→latest, updated 2026-08-13).
- ABS Intl Trade tables publish only SITC *section* aggregates (no iron-ore line item) → ABS cannot supply a clean iron-ore series; REQ DISR workbook is the authoritative iron-ore/coal/bauxite/LNG quarterly source.
- ETF: BDRY futures-only class mix 52.2/38.4/9.4 ≈ prospectus 50/40/10; Aug26 C5TCM sensitivity = 155×1000/2.2M sh = $0.0705/sh per $1k/day — README example exact. Stated weightings sum 120.5% = MV-derived exposure (leverage disclosed).
